import React from 'react';
import './Product.css';

const Product = (props) => {

    console.log(props.product);

    return (
        <div>
            
        </div>
    );
};

export default Product;